# QA Report — GAP-036 修復射不出鋼針

> 日期：2026-06-20 | 角色：QA | 類型：GAP-035 場景放大回歸 hotfix

## 1. 自動化驗證

| 項目 | 結果 |
|------|------|
| `sensor-scan.ps1` | ✅ 21/21 PASS |
| Godot `--check-only` | ✅ 0 errors |
| run_project 啟動 | ✅ errors: [] 乾淨啟動 |

## 2. 根因與修復（算術可證）

- **根因**: `needle_projectile.gd` escape 邊界硬編碼舊 960×540（`x>1024, y>604`）。GAP-035 放大場景後玩家出生 `y=653`，針出生即 `653 > 604` → 第一幀 `queue_free` → 射出即消失。
- **修復**: 邊界改 `get_viewport_rect().size`（1280×720）±64。
- **驗算**:
  - 修復前：出生 y≈653 **> 604** → 被刪（bug）。
  - 修復後：出生 y≈653 **< 784**（720+64）→ 存活、正常飛行。✅
  - x 方向：出生 x≈656 **< 1344**（1280+64）→ 存活。✅

## 3. 手動驗證（需玩家確認）

| # | 操作 | 預期 |
|---|------|------|
| 1 | 左鍵點擊 | 射出攻擊針並飛行/命中 |
| 2 | 右鍵 hold + 左鍵 | 射出帶線針並建立錨點（彈力繩拉近）|
| 3 | 連續射 3 根 | 受 max_needles=3 限制 |

## 4. 結論

- 自動化全 PASS；根因明確、修復算術可證。
- 邊界改為視窗推導，避免日後改場景尺寸再次回歸。
- 實際射針請玩家於遊戲確認（情境 1–3）。
- **QA 通過**。
